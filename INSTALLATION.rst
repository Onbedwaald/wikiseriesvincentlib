============
Installation
============

At the command line::

    $ pip install wikiseriesvincentlib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv wikiseriesvincentlib
    $ pip install wikiseriesvincentlib

Or, if you are using pipenv::

    $ pipenv install wikiseriesvincentlib

